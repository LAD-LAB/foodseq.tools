% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_common_names.R
\name{assign_common_names}
\alias{assign_common_names}
\title{Assign Common Names to a Phyloseq Object}
\usage{
assign_common_names(
  physeq,
  common_names_csv,
  report_conflicts = TRUE,
  report_all_conflicts = TRUE,
  concatenate_conflicts = TRUE
)
}
\arguments{
\item{physeq}{A phyloseq object}

\item{common_names_csv}{Path to a CSV with columns: \code{asv}, \code{conventional_name},
\code{taxon}. Optional columns: \code{genus}, \code{genus_conventional_name} (for
genus-level conflict resolution).}

\item{report_conflicts}{If TRUE, print a summary of multi-match conflicts}

\item{report_all_conflicts}{If TRUE, report all conflicts; if FALSE, only
report unresolved ones}

\item{concatenate_conflicts}{If TRUE, concatenate unresolved conventional
names using smart merge; if FALSE, use the first match}
}
\value{
The phyloseq object with \code{common_name} and \code{taxa} columns added to
its tax_table. Multi-match conflict details are stored as an attribute
\code{"common_name_conflicts"}.
}
\description{
Matches ASV sequences in a phyloseq's tax_table to a
reference CSV of common names, adding \code{common_name} and \code{taxa} columns.
Handles multi-match conflicts via superset logic, genus-level
resolution, and smart name merging. For ASVs with no CSV match but a
species-level (or below) taxonomy assignment, common names are
propagated from sibling ASVs that share the same taxonomic
classification, provided all siblings agree on a single unambiguous
name.
}
